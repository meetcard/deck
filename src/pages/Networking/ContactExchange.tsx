import { PagePlaceholder } from '../PagePlaceholder'

export function ContactExchange() {
  return (
    <PagePlaceholder
      title="Contact Exchange"
      description="The two-way exchange flow when two people connect in person."
    />
  )
}
