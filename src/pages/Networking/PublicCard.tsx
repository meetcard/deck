import { PagePlaceholder } from '../PagePlaceholder'

export function PublicCard() {
  return (
    <PagePlaceholder
      title="Public Card"
      description="The public-facing MeetCard shown when sharing a link or QR code."
    />
  )
}
