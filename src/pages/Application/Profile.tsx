import { PagePlaceholder } from '../PagePlaceholder'

export function Profile() {
  return (
    <PagePlaceholder
      title="Profile"
      description="The signed-in user's own MeetCard — name, title, company, and contact details."
    />
  )
}
