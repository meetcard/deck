import { PagePlaceholder } from '../PagePlaceholder'

export function AppShell() {
  return (
    <PagePlaceholder
      title="App Shell"
      description="The persistent AppBar + BottomNav shell every authenticated screen renders inside."
    />
  )
}
