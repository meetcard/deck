import { PagePlaceholder } from '../PagePlaceholder'

export function Connections() {
  return (
    <PagePlaceholder
      title="Connections"
      description="Browse by the moment you met — one stack of cards per event."
    />
  )
}
