import { PagePlaceholder } from '../PagePlaceholder'

export function Leads() {
  return (
    <PagePlaceholder
      title="Leads"
      description="Connections flagged as sales or hiring leads, tracked through follow-up."
    />
  )
}
